class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        num = ""
        for i in digits:
            num += str(i)
        
        num = int(num)
        num += 1
        num = str(num)
        
        res = []
        for i in num:
            res.append(int(i))
        return res
        
